#!/usr/bin/env bash
set -e
cd "$(dirname "$0")/.."

echo "Running format check..."
uv run ruff format --check .

echo "Running lint check..."
uv run ruff check .

echo "Running tests..."
PYTHONPATH=. uv run pytest
